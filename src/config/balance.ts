/**
 * All tunable game numbers live here.
 * No game-logic magic numbers anywhere else.
 */

export const TIME = {
  GAME_HOURS_PER_DAY: 15,
  REAL_MIN_PER_GAME_HOUR: 5,
  DEMO_SPEED_MULT: 30,
  DAY_START_HOUR: 0.0,
  DAYLIGHT_HOURS: 8,
  NIGHT_HOURS: 7,
} as const;

export const TIME_GAME_HOURS_PER_REAL_SEC =
  (1 / (TIME.REAL_MIN_PER_GAME_HOUR * 60)) * TIME.DEMO_SPEED_MULT;

export const STATS = {
  MAX: 100,
  FOOD_DECAY_PER_HR: 1.2,
  DRINK_DECAY_PER_HR: 1.8,
  STAMINA_SPRINT_DRAIN_PER_SEC: 12,
  STAMINA_REGEN_PER_SEC: 6,
  STARVE_ARMOR_DRAIN_PER_HR: 4,
  LOW_THRESHOLD: 25,
} as const;

export const PLAYER = {
  WALK_SPEED: 5,
  SPRINT_SPEED: 9,
  JUMP_VELOCITY: 7,
  GRAVITY: 18,
  EYE_HEIGHT: 1.65,
  THIRD_PERSON_DIST: 4.0,
  THIRD_PERSON_HEIGHT: 1.2,
  MOUSE_SENS: 0.0024,
  PITCH_LIMIT: 1.4,
  SPAWN_RING_DIST: 75,
} as const;

export const MUTANT = {
  COUNT: 6,
  PATROL_SPEED: 1.5,
  CHASE_SPEED_MULT: 1.6,
  DETECTION_RADIUS: 14,
  NIGHT_DETECTION_BONUS: 1.6,
  REPICK_MIN_SEC: 3,
  REPICK_MAX_SEC: 7,
  PATROL_RADIUS: 6,
  HOVER_HEIGHT: 1.5,
  HOME_RANGE: 100,
  HEAD_VAR_2_PROB: 0.35,
  ARM_VAR_3_PROB: 0.4,
  LEG_VAR_3_PROB: 0.3,
} as const;

export const WORLD = {
  TREE_COUNT: 80,
  ROCK_COUNT: 30,
  STAR_COUNT: 600,
  OCEAN_RADIUS: 200,
  TERRAIN_SIZE: 220,
  TERRAIN_SEGMENTS: 140,
  ISLAND_RADIUS: 88,
  TREE_RING_INNER: 8,
  TREE_RING_OUTER: 78,
  ROCK_RING_INNER: 5,
  ROCK_RING_OUTER: 83,
} as const;

export const HUD = {
  THREAT_DANGER_DIST: 8,
  THREAT_WARY_DIST: 18,
  MAP_RANGE: 100,
  BANNER_DEFAULT_MS: 2400,
  BANNER_INTRO_MS: 3200,
  FOG_NEAR: 60,
  FOG_FAR: 180,
} as const;

export const COLORS = {
  TERRAIN_SAND: 0xc7a36a,
  TERRAIN_GRASS: 0x4a6a32,
  TERRAIN_ROCK: 0x6f6256,
  OCEAN: 0x2a5070,
  TREE_TRUNK: 0x4a3220,
  TREE_LEAVES: 0x2c5230,
  ROCK: 0x6a625a,
  SUN: 0xffe7c0,
  MOON: 0x8090c0,
  HEMI_SKY: 0xb6cce0,
  HEMI_GROUND: 0x3c2a18,
  SKY_DAY: 0x87aabd,
  SKY_SUNSET: 0xd6814a,
  SKY_NIGHT: 0x0a1226,
  PLAYER_SKIN: 0xb98c5a,
  PLAYER_SHIRT: 0x6b4226,
  PLAYER_PANTS: 0x33251a,
  MUTANT_SKIN_BASE: 0x735846,
  MUTANT_SKIN_VAR_RANGE: 0x303030,
  MUTANT_PANTS: 0x3a2a1c,
} as const;
